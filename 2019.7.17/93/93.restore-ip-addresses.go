package problem93

/*
 * @lc app=leetcode id=93 lang=golang
 *
 * [93] Restore IP Addresses
 */
func restoreIpAddresses(s string) []string {
	ans := []string{}
	tmp := []string{}
	for a := 1; a <= 3; a++ {
		for b := 1; b <= 3; b++ {
			for c := 1; c <= 3; c++ {
				for d := 1; d <= 3; d++ {
					if a+b+c+d == len(s) {
					
					}
				}
			}
		}
	}
}
